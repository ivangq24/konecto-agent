##############################################
# Konecto Frontend - .env.example
#
# Copy this file to `.env` in the `frontend` folder
# and fill in the real values for your environment.
##############################################

########################
# API CONFIGURATION
########################
# Backend FastAPI URL reachable from the browser
# In Docker Compose, the backend service is named "backend"
NEXT_PUBLIC_API_URL="http://backend:8000"

# For local development outside Docker you can override with:
# NEXT_PUBLIC_API_URL="http://localhost:8000"